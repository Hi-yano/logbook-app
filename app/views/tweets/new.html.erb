<%= render "shared/mypage" %>
<div class="contents row">
  <div class="container">
    <h3>投稿する</h3>
      <%= form_with(model: @tweet, local: true) do |f| %>
        <%= f.file_field :image, id:"log-image" %>
        <%= f.text_area :text, placeholder: "ポイントの感想など", rows: "10" %>
        <%= f.submit "SEND" %>
      <% end %>
  </div>
</div>